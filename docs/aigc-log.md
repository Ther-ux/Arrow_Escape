# AIGC 开发记录

## AI-01 / 2026-09-13 20:25（Asia/Shanghai）

- **子任务：** M1 核心逻辑：Arrow 数据模型、四方向路径检测与自动化测试。
- **使用工具：** Codex。
- **我的请求：** “进行第一阶段的开发”。根据项目阶段计划，将第一阶段落实为 M1：先覆盖四方向、边界、近远阻挡、无关行列和移除 blocker，再实现纯路径逻辑，不改 UI。
- **AI 提供：** 新建不可变 `Arrow` 模型和 `Direction` 枚举；实现只扫描箭头前方射线的 `is_path_clear()`；补充对应 pytest 用例及开发依赖说明。
- **实际运行结果：** 初始测试先因实现模块尚未创建而无法收集，符合测试先行预期。首轮实现又触发 `AbstractSet` 导入错误；改为从 `typing` 导入后，pytest 共 34 项通过，`compileall` 通过。
- **发现的问题：** `AbstractSet` 不应从 `collections.abc` 导入；首轮测试实际捕获该错误。
- **人工修改：** 本轮没有用户提供的代码修改；实现按测试结果修正导入。用户可在后续复核数据模型和边界约定。
- **验证方式：** `.venv\Scripts\python.exe -m pytest -q`（34 passed）；`.venv\Scripts\python.exe -m compileall -q core tests`（通过）。
- **关联 Commit / 截图：** `feat: 实现四方向路径检测与核心单元测试`；本阶段没有 UI，因此未生成截图。

## AI-02 / 2026-09-15 22:53（Asia/Shanghai）

- **子任务：** M2 棋盘数据和游戏状态、动画计时、碰撞失误、重开与切关。
- **使用工具：** Codex。
- **我的请求：** “开始开发”；开发期间补充“记得关键的commit”。沿 M1 继续实施 M2，并按可验证节点分别提交。
- **AI 提供：** 纯逻辑 `Board`、`Game`、帧时间推进的动画数据；三个原创 MVP 布局；T01–T06、最后一箭、动画期间重复点击、末关保护等状态测试。
- **实际运行结果：** `.venv\Scripts\python.exe -m pytest -q`，48 项通过，包含 M1 的 34 项测试与新状态测试。
- **发现的问题与处理：** 碰撞动画期间连续点击同一箭头会重复触发反馈的风险，通过动画锁定该箭头并测试验证；最后一次碰撞后立即禁止新点击，待反馈完成进入失败页。
- **人工修改：** 用户要求保留关键 Commit；本轮代码由 Codex 编写，未收到用户代码修改。
- **验证方式：** 自动化状态与路径测试；三个 MVP 布局在状态模型中按合法顺序清空。尚未进行人工 UI 试玩；六关难度和独立求解器留到 M3。
- **关联 Commit / 截图：** `feat: 增加棋盘状态、碰撞失误与关卡重开流程`；界面与截图将在下一节点验证。

## AI-03 / 2026-09-15 22:55（Asia/Shanghai）

- **子任务：** M2 Pygame 界面、窗口事件、动效与截图验证。
- **使用工具：** Codex。
- **我的请求：** 继续“开始开发”，并落实用户“记得关键的commit”的要求。
- **AI 提供：** `main.py` 主入口，暗色开始/游戏/结果页面，共用几何换算的 `BoardLayout`，程序绘制箭头，450 ms 加速淡出与 320 ms 碰撞反馈，按钮/键盘操作；界面事件测试与可复现窗口截图脚本。
- **实际运行结果：** 53 项 pytest 测试通过；`compileall` 通过；`python -m tools.smoke_ui` 使用 `windows` SDL 驱动，注入鼠标事件，依次通过 L1/L2/L3 和切关、失败、重开、关闭窗口，生成八张截图。
- **发现的问题与处理：** 代码审查发现返回首页与下一次绘制之间可能残留上一页按钮命中区域，增加当前页面的允许操作过滤并补回归测试。截图检查后为 6×6 布局增加标题间距；失误显示增加数字，便于直接读取。
- **人工修改：** 没有用户代码修改；代码由 Codex 编写与修正，截图由 Codex 检查。
- **验证方式：** pytest 界面用 SDL dummy 驱动；独立脚本实际创建 Windows Pygame 窗口并走事件队列，进行自动化完整通关。查看开始页、棋盘、碰撞、飞出、三个通关页与失败页截图，中文和按钮无明显重叠。
- **关联 Commit / 截图：** `feat: 完成Pygame棋盘界面与非阻塞动画交互`；`docs/screenshots/`。这些验证不是人工试玩记录；作业要求的本人逐关试玩仍待完成。

## AI-04 / 2026-09-15 23:43（Asia/Shanghai）

- **子任务：** 受轨迹约束的柔性尾线与分离布局。
- **使用工具：** Codex。
- **我的请求：** “实现箭头拖拽柔性线条飞行”，优先 Verlet Integration + distance constraints；过程中补充“线不要重叠在一起”、重述近端先牵引、远端渐进跟随的核心效果，以及“不是乱甩”。
- **AI 提供：** 固定 240 Hz 的 Verlet 节点链、距离约束、较强阻尼、历史轨迹软引导和 4 px 横向约束；Catmull–Rom 曲线和 2 倍采样渲染；统一避让头部与离场走廊的尾线规划；等待尾线离场后释放下一支；重开/切关清除模拟；独立转向示例与 GIF。
- **实际运行结果：** 68 项 pytest 测试通过，`compileall` 通过；Windows Pygame 窗口脚本完成三个关卡、切关、失败、重开、退出；转向示例捕获 121 帧并生成 GIF。静止尾线间距测试均大于 15 px，30/144 fps 模拟一致性和 4 px 横向轨迹约束测试通过。
- **发现的问题与处理：** 开始时已有未提交的尾线外观和截图，既有尾线独立生成导致重复占位，改为统一分配分离路径。首次曲线切线检查以 20 个离散采样近似导数，未满足阈值，使用 100 个采样检查连续切线后通过。首次将 4 px 约束施加到节点目标位置，导致远端过早前移，近端/远端先后响应测试失败；改为只约束到最近历史线段的横向距离，保留纵向滞后后通过。
- **人工修改：** 用户明确要求避免重叠与乱甩；代码由 Codex 按这些要求调整，未收到用户代码补丁。开始时已有的地图边界和配色沿用，旧截图修改保留在工作区，没有覆盖。
- **验证方式：** 路径、状态、Verlet 跟随/转向/阻尼、曲线、分离布局、界面事件及末箭等待尾线结束等自动化测试；查看游戏初始/飞出/碰撞截图与转向示例不同阶段帧。自动 UI 仍不算本人手工试玩。
- **关联 Commit / 截图：** `feat: 实现受轨迹约束的柔性尾线与分离布局`；`docs/rope-evidence/`。独立转向示例用于验证曲线响应，游戏箭头仍按其固定方向离场。
